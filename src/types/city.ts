import { Location } from './location';

export type City = {
  name: string;
  location: Location;
};

export type CityList = {
  Paris: string;
  Cologne: string;
  Brussels: string;
  Amsterdam: string;
  Hamburg: string;
  Dusseldorf: string;
};
